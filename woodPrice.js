function woodPies(tableCount, chirCount, badCount){
    const totalTablePrice = tableCount * 1200;
    const totalChirPrice = chirCount * 900;
    const totalBadPrice = badCount * 2000;
    
    const woodPrice = totalBadPrice + totalChirPrice + totalTablePrice
    return woodP
}
const price = woodPies (4, 2, 1)
console.log(woodPies)
